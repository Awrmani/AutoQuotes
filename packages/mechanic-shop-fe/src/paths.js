const login = () => ({ pathname: '/login' });

const dashboard = () => ({
  pathname: '/dashboard',
});

const inventory = () => ({
  pathname: '/inventory',
});

const itemForm = () => ({
  pathname: '/itemForm',
});

const paths = { login, dashboard, inventory, itemForm };

export default paths;
