const login = () => ({ pathname: '/login' });

const dashboard = () => ({
  pathname: '/dashboard',
});

const inventory = () => ({
  pathname: '/inventory',
});

const paths = { login, dashboard, inventory };

export default paths;
